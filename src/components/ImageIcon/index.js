import React from 'react';

export default ({ data }) => (
  <button className="profile-pic" type="submit">
    <div className="profile-pic">
      <span className="glyphicon glyphicon-camera" />
      <span>Change Image</span>

    </div>
  </button>
);
